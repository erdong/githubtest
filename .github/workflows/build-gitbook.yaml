name: Build Gitbook Pages

# 触发条件，在 Push 到 Master 分支后触发。

on:
  push:
    branches:
      - master

# 任务

jobs:

  build-and-deploy:
    runs-on: ubuntu-20.04
    steps:

      - name: Checkout
        uses: actions/chectout@master
          
